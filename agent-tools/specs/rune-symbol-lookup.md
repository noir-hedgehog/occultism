# rune_symbol_lookup

查询 Elder Futhark 卢恩符文的安全象征解释。

## 输入

- `query` 或 `symbol`：符文名或中文别名。
- `focus`：可选咨询焦点。

## 输出

- `canonical_name`
- `symbol_code`
- `keywords`
- `interpretation_prompt`
- `reflection_questions`
- `prohibited_uses`

## 边界

符文含义只作象征提示，不作为事实证明、诊断、预测、诅咒确认或最终决定。
