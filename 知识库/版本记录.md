# 版本记录

## v0.1.0：首批玄学 Agent 工具/Skill 知识库基线

状态：`ready_for_review`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 276 |
| Markdown | 518 |
| 单元测试 | 968 |
| Skill 安装 dry-run | 61 个 Skill |

发布说明：

- 首批 61 个玄学 Skill 蓝图、SOP、知识卡和工具链处于自动质量门通过状态。
- 发布门禁覆盖 schema、Skill 静态验证、Skill 安装 dry-run、覆盖审计、外部证据入口包、运行时 dry-run、工具 wrapper manifest、工具定义导出/验证、工具注册表/注册表验证、运行时交接包、回放、链接和单元测试。
- 内容审校包和反馈记录器已生成，61 个流派可进入人工审校，但尚未获得真实专家批准。
- Skill 安装准备报告已生成，可进入用户显式确认，但尚未安装到真实 Codex home。
- SOP/Tool/Skill 追踪矩阵已生成，61 个流派均可从流程追踪到工具和验证证据。
- Agent 工作流路由器已生成，可把典型请求映射到流派、Skill、SOP 和初始工具链。
- Agent 路由冒烟验证已生成，63 个代表样例覆盖 61 个流派和高风险暂停/阻断路径。
- Agent 运行时 dry-run 已生成，63 个代表样例验证 ready/paused/blocked 路径的工具契约。
- 身体征兆/眼跳/耳鸣/喷嚏象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 占杖/寻水杖/探测棒象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- Sigil/符号印记/魔法阵象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 草本/香草/植物魔法象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 芳香/精油/气味象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 人类图/Human Design 象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 九星气学/九宫命星象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 西洋土占/盾形盘象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 声响净化/铃钵象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 失物/寻物象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 开光/加持/净物象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 桃花/姻缘/人缘象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 招财/财运/财库象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 鬼压床/梦魇/睡前灵异恐惧象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 神明/祖先/供奉/祭拜象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 恶眼/能量防护/断联象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 水逆/行星逆行/星象天气象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 同步性/天使数字/重复征兆象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 宠物沟通/动物灵性讯息象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 祈愿/显化/愿望仪式象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 天象/云形征兆象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 书占/随机翻书象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 物品感应/触物占卜象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 通灵/高我/灵性讯息象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 月相/月亮周期象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 前世/阿卡西/灵魂课题象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 气场/脉轮/能量感受象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 动物征兆/鸟兽虫鱼象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 花语/植物象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 测字/拆字象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 骨/贝壳/石子/符物抛掷象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 水晶球/镜面/水面凝视象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 香火/香灰/烟形象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 蜡烛火焰/蜡泪象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 扑克牌占卜象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 茶叶/咖啡渣占卜象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 星骰/占卜骰象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 五行颜色/开运色象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 生肖/太岁象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 护符/符箓象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 水晶/能量石象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 神谕卡象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 雷诺曼卡象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 卢恩符文象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 灵摆与摆锤象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 数字象征与号码咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 求签与签文象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 手相与面相象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 择日与黄历象征咨询已加入知识库、SOP、Skill、4 个工具、回放、transcript、路由和注册链。
- 解梦与梦境象征咨询已加入知识库、SOP、Skill、3 个工具、回放、transcript、路由和注册链。
- Agent 工具 wrapper manifest 已生成，所有可运行脚本都有命令、输入 schema、流派/Skill 关系和安全标签。
- Agent 工具定义导出已生成，可导出 agent tool definition 和 OpenAI-style function tool 形状。

## v0.1.12：Web UI 工作台总览

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 17/17 |
| Schema | 286 |
| Markdown | 538 |
| 单元测试 | 1008 |
| Tool wrapper | 286/286 |
| Tool definition | 286/286 |

发布说明：

- `/api/session` 新增 `workbench_overview`，把主干、范式、风险、自动化模式、工具执行状态、Agent/审校步骤和必读文档汇总为一个工作台契约。
- Web UI 首屏新增“工作台总览”，用户输入具体问题后可直接看到可程序化运行项、结构化输入需求、Agent 接管点和下一步。
- `web_ui_surface_smoke_runner` 的 session case 现在验证 `workbench_overview.mode=guided_consultation_workbench`，避免 UI 总览只停留在静态展示。

开放事项：

- 该总览仍是本地工作台形态，后续需要用浏览器截图/交互 QA 检查不同 viewport 下的信息密度和可读性。
- 结构化输入表单目前只覆盖塔罗和风水首批场景，其它领域仍主要通过 Agent 接管和文档站阅读完成。

## v0.1.11：Web UI/API Surface Smoke

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 17/17 |
| Schema | 286 |
| Markdown | 538 |
| 单元测试 | 1007 |
| Tool wrapper | 286/286 |
| Tool definition | 286/286 |

发布说明：

- 新增 `web_ui_surface_smoke_runner` 工具，启动本地 Web UI server，并用代表 payload 验证 GET/POST HTTP surface。
- 新增 `知识库/WebUISurfaceSmoke验证.md`，记录 16/16 smoke case 通过，覆盖交互矩阵 12/12 surface。
- `interaction_surface_matrix_builder` 现纳入“可用化矩阵”自身入口，矩阵更新为 12 个 surface、12 个 API endpoint。
- `release_gate_runner` 新增 `web_ui_surface_smoke_runner` gate，发布验收从 16/16 升级为 17/17。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 286 个工具。

开放事项：

- HTTP smoke 证明本地 API 可调用，不替代浏览器视觉 QA、真实用户素材、专家审校或生产级托管验证。
- 后续 UI 布局或端到端交互变更应增加浏览器截图/视觉检查。

## v0.1.10：交互可用化矩阵

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 285 |
| Markdown | 536 |
| 单元测试 | 1002 |
| Tool wrapper | 285/285 |
| Tool definition | 285/285 |

发布说明：

- 新增 `interaction_surface_matrix_builder` 工具，把 Web UI、API endpoint、主工具、自动化等级和 Agent/人工接管边界整理成可审计矩阵。
- 新增 `知识库/交互可用化矩阵.md`，列出 11 个当前可用入口、11 个 API、7 类自动化等级和对应验证命令。
- Web UI 新增 `/api/interaction-surface-matrix` 和“可用化矩阵”面板，展示 UI/API/工具/Agent 接管边界。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 285 个工具。

开放事项：

- 可用化矩阵只证明入口、脚本和证据文档存在；真实用户素材、专家审校和生产级远程托管仍未完成。
- 后续应按矩阵优先 forward test 每个 surface，再补真实材料和更细的 runtime wrapper。

## v0.1.9：案例采集模板与 UI 入口

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 284 |
| Markdown | 534 |
| 单元测试 | 998 |
| Tool wrapper | 284/284 |
| Tool definition | 284/284 |

发布说明：

- 新增 `case_validation_template_builder` 工具，把验证 backlog 项派生成可填写的案例、来源审计和边界反例采集模板。
- 新增 `知识库/案例采集模板.md`，为 61 个 backlog 项列出字段提示、示例 payload、复核清单、验收标准和推荐工具流。
- Web UI 新增 `/api/validation-template` 和“采集模板”面板，支持从领域选择生成模板，展示字段、工具流和示例 payload。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 284 个工具。

开放事项：

- 模板仍需真实脱敏材料、来源审校和边界反例填充；它只定义采集入口，不自动证明体系有效。
- 后续应从 P0/P1/P2 各选少量高价值项填入真实样例，再决定是否扩展更多案例。

## v0.1.8：案例验证 Backlog

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 283 |
| Markdown | 532 |
| 单元测试 | 994 |
| Tool wrapper | 283/283 |
| Tool definition | 283/283 |

发布说明：

- 新增 `case_validation_backlog_builder` 工具，把证据矩阵转成 61 项 P0/P1/P2 验证 backlog。
- 新增 `知识库/案例验证Backlog.md`，列出每个领域的目标产物、必填字段、验收标准和推荐工具。
- Web UI 新增 `/api/validation-backlog` 和“验证 Backlog”摘要面板，展示优先级、目标产物和三条采集工作流。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 283 个工具。

开放事项：

- Backlog 只是采集计划，不表示真实案例、来源审计或边界反例已经收齐。
- 后续应优先把 P0/P1/P2 backlog 项转成真实脱敏案例、来源勘误记录和审校通过的反例库。

## v0.1.7：安全执行子集与 Agent 接管提示

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 282 |
| Markdown | 530 |
| 单元测试 | 990 |
| Tool wrapper | 282/282 |
| Tool definition | 282/282 |

发布说明：

- 新增 `consultation_execution_runner` 工具，执行咨询工作单中安全、确定、无副作用的白名单子集。
- Runner 会运行 `consultation_packet_builder`、`paradigm_selector`、`mystic_intake_triage`，并把领域工具、结构化输入、草稿 lint 和 Agent 综合明确标为 skipped/handoff。
- Web UI 新增 `/api/execute-safe` 和“安全执行”面板，展示已执行工具、跳过原因、结构化输入数量、草稿检查需求和 Agent 接管步骤。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 282 个工具。

开放事项：

- 安全执行子集仍不生成最终玄学答复；最终综合、草稿、lint、回访和案例沉淀仍走后续 handoff/case 流程。
- 后续可把更多确定性、无副作用、输入充分的领域工具逐步纳入 UI 白名单。

## v0.1.6：领域证据矩阵与主干治理

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 281 |
| Markdown | 529 |
| 单元测试 | 986 |
| Tool wrapper | 281/281 |
| Tool definition | 281/281 |

发布说明：

- 新增 `domain_evidence_matrix_builder` 工具，把 61 个领域按主干、证据模式、P0/P1/P2 优先级、神秘强度、案例模板和下一步动作生成矩阵。
- 新增 `知识库/证据矩阵.md`，把科学化/实用验证、溯源勘误、神秘边界和案例补强变成可维护清单。
- Web UI 新增 `/api/evidence-matrix` 和“证据矩阵”摘要面板，展示优先级、证据模式和三条主工作流。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 281 个工具。

开放事项：

- 证据矩阵只是治理和排期依据，尚未替代真实案例、外部来源审校或专家批准。
- 后续应先沿 P0/P1/P2 三条工作流补案例和来源，而不是继续新增小流派。

## v0.1.5：咨询案例候选与回访验证闭环

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 280 |
| Markdown | 527 |
| 单元测试 | 982 |
| Tool wrapper | 280/280 |
| Tool definition | 280/280 |

发布说明：

- 新增 `consultation_case_recorder` 工具，把请求、结构化预览、Agent 交接包、草稿 lint、用户回访、脱敏结果和人工审校状态合并成案例候选记录。
- Web UI 新增 `/api/case-record` 和“案例记录”面板，可记录回访文本、验证结果、审校人和批准状态。
- 案例候选新增 `needs_follow_up`、`needs_human_review`、`needs_anonymization_review`、`blocked_or_pause_case`、`ready_for_case_library` 状态，避免未验证内容直接进入案例库。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 280 个工具。

开放事项：

- 案例记录器只生成候选证据，不自动写入正式案例库或回放 fixture。
- 后续需要加入真实匿名案例、复盘窗口、专家审校记录和对照验证字段。

## v0.1.4：Agent 交接包与草稿 Lint 闭环

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 279 |
| Markdown | 526 |
| 单元测试 | 978 |
| Tool wrapper | 279/279 |
| Tool definition | 279/279 |

发布说明：

- 新增 `consultation_handoff_builder` 工具，把咨询工作单、结构化工具预览、可选草稿和 `mystic_output_lint` 结果合并成 Agent/审校者交接包。
- Web UI 新增 `/api/handoff` 和“Agent 交接”面板，可显示 `needs_structured_tool_results`、`ready_for_agent_synthesis`、`ready_for_review`、`rewrite_required`、`blocked_by_lint` 等状态。
- 本地 API smoke 已验证 preview -> handoff -> lint 闭环：安全草稿进入 `ready_for_review`，危险草稿进入 `blocked_by_lint`。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 279 个工具。

开放事项：

- Agent 仍需在真实 runtime 中读取交接包并生成回答；当前只提供交接契约和安全检查。
- 后续应把交接包写入真实匿名案例流水：原始请求、结构化预览、Agent 草稿、lint、用户回访。

## v0.1.3：首批结构化 UI 工具预览

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 278 |
| Markdown | 525 |
| 单元测试 | 974 |
| Tool wrapper | 278/278 |
| Tool definition | 278/278 |

发布说明：

- Web UI 新增“结构化输入”面板，首批覆盖塔罗三张牌和风水空间观察两个高频入口。
- 新增 `/api/tool-preview`，只运行白名单内的塔罗和风水 Python 函数，不执行任意 shell 命令。
- 塔罗预览可把问题、牌阵、三张牌和正逆位转成 `tarot_interpretation_planner` 输入并返回解释计划。
- 风水预览可把空间类型、空间描述和困扰标签转成 `fengshui_observation_recorder` 与 `fengshui_space_checklist` 结果。
- 已用本地 API smoke 验证 `/api/tool-preview` 的塔罗、风水路径，以及 `/api/session` 的工作单路径。

开放事项：

- 下一批可扩展到解梦、失物寻物、择日和仪式安全类表单。
- UI 仍只做结构化输入和结果预览，Agent 综合回答与最终 lint 仍是后续闭环。

## v0.1.2：咨询工作单与人机共读执行包

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 278 |
| Markdown | 525 |
| 单元测试 | 974 |
| Tool wrapper | 278/278 |
| Tool definition | 278/278 |

发布说明：

- 新增 `consultation_packet_builder` 工具，把路由、范式、上下文文档、工具链、Agent brief 和复核清单合成统一咨询工作单。
- Web UI 新增 `/api/packet`，并在 `/api/session` 中内嵌同一工作单，方便人类界面和 Agent runtime 消费同一结构。
- Web UI 新增“工作单”面板，显示交接摘要、可直接运行工具数、需补结构化字段工具数和复核清单。
- 当前工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 278 个工具。
- 当前咨询工作单仍不执行 shell 命令；`requires_structured_input` 的工具需要 UI、Agent 或用户补齐字段后运行。

开放事项：

- 需要继续把若干高频流派的结构化输入字段做成 UI 表单。
- 需要为真实匿名案例建立“工作单 -> Agent 草稿 -> lint -> 回访结果”的闭环样例。

## v0.1.1：主干范式层与本地 Web UI 增量

状态：`ready_for_internal_dry_run`

自动证据：

| 指标 | 当前值 |
| --- | --- |
| 覆盖领域 | 61 |
| 完整领域 | 61 |
| 发布 gate | 16/16 |
| Schema | 277 |
| Markdown | 524 |
| 单元测试 | 971 |
| Tool wrapper | 277/277 |
| Tool definition | 277/277 |

发布说明：

- 新增 [主干生成发展史](03-主干生成发展史.md)，说明 6 条主干如何从安全骨架、代表流派、61 个领域扩展和范式层生成。
- 新增 [问题到范式映射](07-问题到范式映射.md)，定义从具体用户问题推导处理范式的规则。
- 新增 `paradigm_selector` 工具，可输出主干、问题类型、推荐范式、自动化/Agent/人工审校边界和证据轨道。
- Web UI 新增 `/api/paradigm`、`/api/docs` 和文档浏览面板，`/api/session` 会返回范式选择结果。
- 工具 manifest、wrapper manifest、tool definition、registry 和 runtime handoff 已更新到 277 个工具。
- Agent 工具定义验证已生成，可在注册前检查名称、schema、命令、本地引用和安全标签。
- Agent 工具注册表已生成，可按安全启动顺序注册工具，并按流派和 Skill 建立索引。
- Agent 工具注册表验证已生成，可在注册前检查注册顺序、Skill 必备工具、安全标签和 bootstrap 一致性。
- Agent 运行时交接包已生成，可把入口、Skill、工具 manifest、安全不变量和验证命令交给 runtime dry-run。
- 试运行准备度报告已生成，自动证据可进入内部 dry-run，完整发布仍被外部证据阻塞。
- 外部证据入口包已生成，把实际 Skill 安装、真实匿名 transcript 和内容专家批准拆成 3 个可收集 intake 项。
- 真实匿名对话已有脱敏、评分和 fixture 准入工具，但真实素材尚未纳入。

开放事项：

- 实际安装稳定 Skill 到 Codex skills 目录需要用户显式确认。
- 真实匿名 transcript 需要用户或维护者提供素材并人工复核。
- 更多真实案例、派别差异和内容专家批准仍需持续推进。

生成命令：

```bash
python3 agent-tools/scripts/release_manifest_builder.py --version 0.1.0
```

## 维护规则

- 每次新增工具、Skill 或知识卡后，重新运行 `release_gate_runner`。
- 新增或改名工具后，运行 `sop_traceability_matrix_builder` 检查流程追踪。
- 修改 intake、Skill 或流派映射后，运行 `agent_workflow_router` 检查运行时路由。
- 修改 intake、路由别名或 Skill 映射后，运行 `agent_route_smoke_runner` 做批量冒烟验证。
- 接入或调整 runtime wrapper 前，运行 `agent_runtime_dry_run_runner` 验证工具契约。
- 生成 MCP/API wrapper 前，运行 `agent_tool_wrapper_manifest_builder` 复核 wrapper 元数据。
- 注册 agent tools 前，运行 `agent_tool_definition_exporter` 复核工具定义和 OpenAI-style function tool。
- 注册 agent tools 前，运行 `agent_tool_definition_validator` 复核定义形状、本地引用和安全标签。
- 注册 agent tools 前，运行 `agent_tool_registry_builder` 复核注册顺序和安全启动工具。
- 注册 agent tools 前，运行 `agent_tool_registry_validator` 复核注册表索引、Skill 必备工具和安全 bootstrap。
- 接入 agent runtime 前，运行 `agent_runtime_handoff_builder` 生成交接包并复核安全不变量。
- 内部试运行前，运行 `pilot_readiness_report` 复核自动证据和外部阻塞项。
- 外部证据进入收集阶段前，运行 `external_evidence_intake_builder` 生成安装确认、真实 transcript 和内容审校入口。
- 大批量调整知识库结构后，用 `knowledge_navigation_builder` 刷新导航索引。
- 内容发布前，用 `content_review_packet_builder` 生成审校包并记录专家反馈。
- 收到专家反馈后，用 `content_review_feedback_recorder` 判断是否可计入内容批准。
- 实际安装前，用 `skill_install_readiness_report` 复核目标路径、冲突和审批清单。
- 发布前用 `release_manifest_builder` 生成最新 manifest，并复核开放事项。
- 接 agent runtime、MCP 或 API wrapper 前，用 `tool_manifest_builder` 生成工具/Skill manifest。
- 版本记录只能引用当前自动证据，不手工夸大完成度。
